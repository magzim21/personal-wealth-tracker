# ─────────────────────────────────────────────────────────────
#  Personal financial data must NEVER be committed to this repo.
#  (Enforced also by .githooks/pre-commit and a CI check.)
# ─────────────────────────────────────────────────────────────
ledger.json
*.ledger.json
ledgerbook-*.json
ledgerbook-*.csv
*wealth*.json
*finances*.json
/data/
backups/

# runtime / build artifacts
personal-wealth-tracker
server
node_modules/

# server config (project-local; was ~/Library/Application Support)
.pwt/

# local editor / launch config (per-machine)
.claude/
